# 问题分析报告

## 发现的问题

### 1. 图片404错误
```
GET https://aity88.online:8443/uploads/images/img-xxx.png → 404
GET https://aity88.online:8443/uploages/img-xxx.png → 404 (拼写错误)
```

**根本原因**:
- 后端上传时保存到: `backend/src/uploads/images/文件名`
- 静态文件服务配置: `app.use('/uploads', express.static('uploads'))`
- 访问路径: `/uploads/images/文件名` → 应该映射到 `uploads/images/文件名` ✅
- 但服务器上可能目录结构不对

### 2. 标签编辑报错
```
TypeError: formData.value.tags.push is not a function
```
**原因**: 后端返回的 `tags` 可能是字符串而非数组
**状态**: ✅ 已修复

### 3. 删除消息500错误
**状态**: ⚠️ 待排查,代码逻辑看起来正常

### 4. 编辑时目标用户未带入
**状态**: ⚠️ 已添加调试日志,等待测试

### 5. 发起讨论时关联消息标题不显示
**状态**: ⚠️ 已添加调试日志,等待测试

---

## 解决方案

### 方案A: 修复服务器目录结构 (推荐)

SSH登录服务器执行:
```bash
# 1. 检查当前目录结构
ls -la /tmp/AITY/backend/src/uploads/

# 2. 如果没有images目录,创建软链接或移动文件
cd /tmp/AITY/backend/src
mkdir -p uploads/images

# 3. 如果图片在uploads根目录,移动到images子目录
# (根据实际情况调整)
mv uploads/*.png uploads/images/ 2>/dev/null || true

# 4. 重启服务
pm2 restart aity-backend
```

### 方案B: 统一上传路径配置 (需要代码修改)

修改 `backend/src/routes/upload.js`:
```javascript
// 当前: 保存到 uploads/images/文件名
const fileUrl = `/uploads/images/${filename}`;

// 改为: 保存到 uploads/文件名
const fileUrl = `/uploads/${filename}`;
```

同时修改上传目录:
```javascript
// 当前
const uploadDir = path.join(__dirname, '../../uploads/images');

// 改为
const uploadDir = path.join(__dirname, '../../uploads');
```

**推荐**: 方案A (保持现有逻辑,只修复目录结构)

---

## 测试清单

修复后需要测试:
- [ ] 图片能正常显示
- [ ] 无CORS错误
- [ ] 编辑消息时目标用户正确带入
- [ ] 发起讨论时显示关联消息标题
- [ ] 删除消息不报500错误
- [ ] 标签切换功能正常
