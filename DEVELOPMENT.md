# AITY VIP 项目开发规范

## 📁 项目结构说明

### 1. **backend** - 后端服务
- **技术栈**: Node.js + Express + MySQL
- **功能**: 提供 RESTful API，数据库操作，文件上传等
- **部署**: 腾讯云服务器 (124.221.119.134)
- **自动化部署**: 使用 `deploy-run.js` 脚本

### 2. **frontend** - H5/Web 前端 (uni-app)
- **技术栈**: Vue 3 + Vite + uni-app
- **支持平台**: H5 (Web), 微信小程序, 支付宝小程序等
- **开发服务器**: `http://localhost:5173`
- **生产环境**: `https://aity88.online:8443`
- **用途**: Web 浏览器访问 + 多端小程序

### 3. **aity-uni-app-v2** - 微信小程序专用
- **技术栈**: Vue 2 + uni-app CLI
- **支持平台**: **仅微信小程序**
- **开发工具**: 微信开发者工具
- **用途**: 专门的微信小程序版本

---

## 🔧 环境配置规范

### 基本原则
- **dev 版本**: 连接本地后台 (`192.168.2.140:3001`) + test 数据库
- **build 版本**: 连接腾讯云远程后端 + 正式数据库

### 前端 (frontend) 环境配置
| 文件 | 环境 | API 地址 | 说明 |
|------|------|----------|------|
| `.env.development` | 开发 | `http://localhost:3000` | 本地后台 + test库 |
| `.env.production` | 生产 | `https://aity88.online:8443` | 腾讯云 + 正式库 |

### 微信小程序 (aity-uni-app-v2) 环境配置
| 文件 | 环境 | API 地址 | 说明 |
|------|------|----------|------|
| `.env` / `.env.development.local` | 开发 | `http://192.168.2.140:3001` | 本地后台 + test库 |
| `.env.production` | 生产 | `https://your-tencent-cloud-api.com` | 腾讯云 + 正式库 |

### 后端 (backend) 环境配置
| 文件 | 环境 | 数据库 | 说明 |
|------|------|--------|------|
| `.env.development` | 开发 | test 数据库 | 本地开发 |
| `.env.production` | 生产 | 正式数据库 | 腾讯云部署 |

---

## 🚀 开发流程规范

### 标准迭代流程
```
代码修改 → 本地dev测试 → 用户验收 → 确认部署 → 更新生产环境
```

### 详细步骤

#### 1️⃣ 本地开发阶段
- 修改代码
- 启动本地后台 (`backend`: `npm run dev`)
- 启动前端 dev 版本:
  - `frontend`: `npm run dev` (H5)
  - `aity-uni-app-v2`: `npm run dev:mp-weixin` (微信小程序)
- 在浏览器或微信开发者工具中测试
- **确保连接的是本地后台和 test 数据库**

#### 2️⃣ 本地测试验收
- 测试所有修改的功能
- 确保没有 bug
- **提示用户进行验收**
- 等待用户确认验收通过

#### 3️⃣ 部署生产环境 (需要用户确认)
- **只有验收通过后**，才能部署生产环境
- 询问用户: "是否更新远程服务器环境（包含生产数据库）？"
- 用户确认后，执行部署:
  - 后端: `node deploy-run.js`
  - 前端: `npm run build` 然后上传到服务器
  - 微信小程序: `npm run build:mp-weixin` 然后上传到微信公众平台

---

## 📦 编译命令

### 后端 (backend)
```bash
# 本地开发
cd backend
npm run dev

# 生产部署 (自动部署到腾讯云)
node deploy-run.js
```

### 前端 (frontend) - H5/多端
```bash
# 本地开发 (H5)
cd frontend
npm run dev
# 访问: http://localhost:5173

# 构建生产版本 (H5)
npm run build:h5
# 输出: dist/

# 构建微信小程序
npm run build:mp-weixin
# 输出: dist/dev/mp-weixin

# 构建支付宝小程序
npm run build:mp-alipay
```

### 微信小程序专用 (aity-uni-app-v2)
```bash
# 本地开发 (微信小程序)
cd aity-uni-app-v2
npm run dev:mp-weixin
# 输出: dist/dev/mp-weixin
# 在微信开发者工具中打开此目录

# 构建生产版本 (微信小程序)
npm run build:mp-weixin
# 输出: dist/build/mp-weixin
# 上传到微信公众平台
```

---

## ⚠️ 重要注意事项

### 1. 禁止直接修改生产环境
- 所有修改必须先经过本地 dev 版本测试
- 验收通过后才能部署到生产环境
- **AI 协助时也必须遵循此流程**

### 2. 数据库操作规范
- 本地开发使用 **test 数据库**
- 生产环境使用 **正式数据库**
- 数据库结构变更需要同步更新两个环境

### 3. 环境配置文件管理
- **不要提交**敏感信息到 git (API密钥、数据库密码等)
- 使用 `.env.example` 作为模板
- 每个环境使用独立的配置文件

### 4. CodeBuddy AI 协助规范
- ✅ **可以**: 修改代码、编译 dev 版本、本地测试、提示验收
- ❌ **禁止**: 直接修改生产环境、跳过验收流程、未经确认就部署

---

## 🔒 安全规范

### 1. API 地址管理
- 开发和生产环境使用不同的 API 地址
- 生产环境 API 地址不要硬编码在代码中
- 使用环境变量管理

### 2. 密钥管理
- 生产环境的 API 密钥、数据库密码等敏感信息
- 不要提交到 git 仓库
- 使用环境变量或密钥管理服务

### 3. 版本管理
- 每次生产部署前打上 git tag
- 保留历史版本以便快速回滚
- 微信小程序需要提交审核

---

## 📝 部署检查清单

### 部署前检查
- [ ] 已在本地 dev 环境测试通过
- [ ] 用户已验收确认
- [ ] 已备份当前生产版本
- [ ] 生产环境配置文件正确
- [ ] 已询问用户是否确认部署

### 部署后检查
- [ ] 生产环境功能正常
- [ ] API 连接正常
- [ ] 数据库读写正常
- [ ] 没有报错信息

---

## 🆘 异常处理

### 如果生产环境出现问题
1. **立即停止部署**
2. **回滚到上一个稳定版本**
3. **在本地 dev 环境排查问题**
4. **修复后重新走验收流程**

### 回滚操作
- 后端: `pm2 rollback` 或 git 回滚 + 重新部署
- 前端: 回滚 git 版本 + 重新构建部署
- 微信小程序: 回滚到上一个审核通过的版本

---

**最后更新**: 2026-04-26  
**维护者**: AITY Team  
**适用项目**: AITY VIP (frontend + aity-uni-app-v2 + backend)
