# AITY VIP 小程序 - 迭代文档 v1.8.x

> 更新时间: 2026-04-23

## 📋 迭代版本记录

### v1.8.4 (当前版本) - 2026-04-23

#### 功能更新

##### 1. 消息列表页优化
- ✅ 已读标识和时间分开两行显示
- ✅ 去掉顶部发布按钮（仅保留右下角悬浮+号按钮）
- ✅ 搜索栏简化为实时搜索
- ✅ 筛选功能精简为两个选项：短线VIP用户、中线VIP用户

##### 2. 消息详情页优化
- ✅ 类型标签和时间放在同一行显示
- ✅ 风险提示词改为紧凑单行布局，不换行

##### 3. 消息筛选组件优化
- ✅ 去掉"推送范围筛选（管理员）"标题
- ✅ 精简选项：全部、短线VIP用户、中线VIP用户

##### 4. 行情中心优化
- ✅ 刷新按钮改为悬浮在右上角，圆形设计带阴影效果
- ✅ 减少误触概率

##### 5. 后端消息类型管理 (新增)
- ✅ 新增 `message_types` 数据表，支持动态管理消息类型
- ✅ 新增消息类型管理 API:
  - `GET /api/message-types` - 获取类型列表
  - `POST /api/message-types` - 创建类型（管理员）
  - `PUT /api/message-types/:id` - 更新类型（管理员）
  - `DELETE /api/message-types/:id` - 删除类型（管理员）
- ✅ 初始化脚本自动创建默认11种消息类型

##### 6. 前端消息类型动态加载 (新增)
- ✅ 消息发布页支持从后端动态加载消息类型
- ✅ 管理员可在发布页直接新增消息类型
- ✅ 新增类型支持自定义颜色和图标

#### 数据库变更

```sql
-- 新增 message_types 表
CREATE TABLE IF NOT EXISTS `message_types` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `type` VARCHAR(50) NOT NULL UNIQUE,
  `label` VARCHAR(100) NOT NULL,
  `color` VARCHAR(20) DEFAULT '#667eea',
  `icon` VARCHAR(50) DEFAULT '',
  `sort_order` INT DEFAULT 0,
  `is_active` TINYINT(1) DEFAULT 1,
  `description` VARCHAR(255),
  `created_at` DATETIME,
  `updated_at` DATETIME
);
```

#### 后端文件变更

| 文件 | 说明 |
|------|------|
| `src/models/MessageType.js` | 新增消息类型数据模型 |
| `src/controllers/messageTypeController.js` | 新增类型管理控制器 |
| `src/routes/messageTypeRoutes.js` | 新增类型管理路由 |
| `src/index.js` | 注册新路由 |
| `migrations/create_message_types_table.sql` | 数据库迁移脚本 |
| `init-db.js` | 初始化脚本 |

#### 前端文件变更

| 文件 | 说明 |
|------|------|
| `src/api/messageType.js` | 新增类型管理API |
| `src/pages/create-message/create-message.vue` | 支持动态类型加载和管理弹窗 |

#### 部署说明

1. **数据库迁移**
   ```bash
   # 在MySQL中执行
   mysql -u root -p 投研图灵室 < migrations/create_message_types_table.sql
   ```

2. **重启后端服务**
   ```bash
   cd backend
   npm run dev
   ```

3. **重新编译小程序**
   ```bash
   npm run build:mp-weixin:cloud
   ```

---

### 历史版本

#### v1.8.3 - 2026-04-22
- 消息列表页UI优化
- 消息发布页Markdown主题优化
- AI内容优化功能完善

#### v1.8.2 - 2026-04-20
- 修复消息类型标签显示问题
- 优化消息筛选功能

#### v1.8.1 - 2026-04-18
- 消息发布功能重构
- 支持关联股票代码
- Markdown内容编辑和预览

#### v1.8.0 - 2026-04-15
- 消息系统基础功能
- 用户权限体系完善
- VIP用户标签管理

---

## 📊 消息类型定义

| type | label | color | description |
|------|-------|-------|-------------|
| position_handle | 持仓处理 | #ef4444 | 持仓处理相关消息 |
| pre_market_comment | 盘前点评 | #f59e0b | 开盘前的市场点评 |
| morning_comment | 早盘点评 | #10b981 | 早盘市场分析 |
| morning_focus | 早盘关注 | #06b6d4 | 早盘重点关注标的 |
| afternoon_comment | 午盘点评 | #8b5cf6 | 午盘市场分析 |
| afternoon_focus | 午盘关注 | #ec4899 | 午盘重点关注标的 |
| close_comment | 收盘点评 | #6366f1 | 收盘总结与分析 |
| risk_warning | 风险提示 | #dc2626 | 风险警示消息 |
| system | 系统消息 | #64748b | 系统通知类消息 |
| important | 重要消息 | #eab308 | 重要通知 |
| daily | 日常消息 | #667eea | 日常消息 |

---

## 🔧 维护指南

### 添加新的消息类型（后台）

1. 通过小程序管理员账号登录
2. 进入"发布消息"页面
3. 点击消息类型旁边的"+ 管理"按钮
4. 填写类型标识（英文）、显示名称、选择颜色
5. 点击"添加类型"

### 常见问题

**Q: 新增的消息类型不显示？**
A: 检查 `message_types` 表中该类型的 `is_active` 字段是否为1

**Q: 删除消息类型失败？**
A: 确保没有消息正在使用该类型

---

*文档版本: v1.8.4*
*最后更新: 2026-04-23*
