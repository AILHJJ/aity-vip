# 发布产物目录

> 说明：这里用于存放本地生成的后端发布包、版本号快照和部署校验信息。

## 目录约定

```text
deploy/
  README.md
  releases/
    <版本号>/
      aity-backend-<版本号>.tar.gz
      sha256.txt
```

## 规则

1. 发布包只作为临时产物和审计留档，不提交到 Git。
2. 每次发布包都按提交号或版本号命名。
3. 对应校验值必须一起保存。
4. 服务器上线仍以 `releases/current` 为准，本目录只管本地归档。

