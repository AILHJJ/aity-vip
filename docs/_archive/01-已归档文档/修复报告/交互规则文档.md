# 🤖 Claude Code 交互规则文档

## 📋 交互完成后的标准流程

### ✅ 每次开发任务完成后，必须执行以下步骤：

#### 1. **代码提交** (必须)
```bash
# 检查Git状态
git status

# 添加修改的文件
git add <files>

# 提交代码（使用规范格式）
git commit -m "type: description"

# 推送到远程仓库
git push origin <branch>
```

**提交格式规范**：
- `feat:` 新功能
- `fix:` 修复bug
- `refactor:` 重构
- `style:` 样式调整
- `docs:` 文档更新
- `test:` 测试相关
- `chore:` 构建/工具相关

#### 2. **服务更新** (根据需要)

**前端更新**：
```bash
# 小程序前端
npm run dev:mp-weixin  # 本地开发
# 或
npm run build:mp-weixin  # 生产构建
```

**后端更新**：
```bash
cd backend

# 安装新依赖（如果有）
npm install

# 重启服务
pm2 restart aity-backend

# 或使用修复脚本
bash fix-backend.sh
```

**数据库更新**（如果有SQL变更）：
```bash
# 执行SQL脚本
mysql -u root -p database_name < update.sql
```

#### 3. **验证清单** (必须)

完成开发后，必须验证：
- ✅ 代码已提交到Git
- ✅ 代码已推送到远程仓库
- ✅ 前端编译成功（无错误）
- ✅ 后端服务正常运行
- ✅ 功能测试通过
- ✅ 无控制台错误

---

## 🎯 不同场景的完整流程

### 场景1：前端页面修改

```bash
# 1. 开发完成后
git status
git add frontend/src/pages/xxx.vue
git commit -m "feat: 修改xxx页面"
git push origin feature/iteration-1

# 2. 验证
# 查看编译输出，确保无错误
# 在微信开发者工具中测试功能
```

**需要更新**：
- ✅ Git代码
- ✅ 前端编译（自动）
- ❌ 后端服务（不需要）
- ❌ 数据库（不需要）

---

### 场景2：后端API修改

```bash
# 1. 开发完成后
cd backend
git status
git add src/controllers/xxx.js src/routes/xxx.js
git commit -m "feat: 添加xxx接口"
git push origin feature/iteration-1

# 2. 如果有新依赖
git add package.json
git commit -m "chore: 添加xxx依赖"
git push

# 3. 更新服务器
ssh user@server
cd /path/to/backend
git pull
npm install  # ← 重要！安装新依赖
pm2 restart aity-backend
pm2 logs aity-backend --lines 20
```

**需要更新**：
- ✅ Git代码
- ❌ 前端编译（不需要）
- ✅ 后端服务
- ❌ 数据库（除非有SQL变更）

---

### 场景3：数据库结构修改

```bash
# 1. 创建SQL迁移脚本
vim backend/migrations/xxx.sql

# 2. 提交代码
git add backend/migrations/xxx.sql
git commit -m "feat: 添加xxx表"
git push

# 3. 更新数据库
ssh user@server
mysql -u root -p database_name < backend/migrations/xxx.sql

# 4. 如果后端代码也改了，重启后端
cd /path/to/backend
pm2 restart aity-backend
```

**需要更新**：
- ✅ Git代码
- ❌ 前端编译（不需要）
- ✅ 后端服务（如果相关代码改了）
- ✅ 数据库

---

### 场景4：配置文件修改

```bash
# 1. 修改配置文件
vim .env  # 或其他配置

# 2. 提交代码（如果是模板示例）
git add .env.example
git commit -m "chore: 更新配置示例"
git push

# 3. 如果是服务器配置，直接更新服务器
ssh user@server
cd /path/to/backend
vim .env  # 编辑实际配置
pm2 restart aity-backend
```

**注意**：
- ⚠️ `.env` 文件通常在 `.gitignore` 中，**不要提交**
- ✅ 只提交 `.env.example` 示例文件

---

## 📊 代码提交模板

### 推荐的Commit Message格式

```
type(scope): subject

body

footer
```

### 示例

#### 新功能
```bash
git commit -m "feat(ai-advisor): 添加实时行情显示

- 新增marketRoutes.js行情代理接口
- 前端添加行情指数条组件
- 支持30秒自动刷新

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

#### Bug修复
```bash
git commit -m "fix(discussion): 修复创建讨论后列表不刷新的问题

问题原因:
- API返回格式与前端期望不匹配
- discussionController返回数组而非对象

解决方案:
- 统一返回格式为 { discussions: [] }
- 前端相应调整数据访问路径

Fixes: #123

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

#### 重构
```bash
git commit -m "refactor(ai-advisor): 优化页面布局和交互

主要改进:
- 简化顶部栏，移除冗余元素
- 优化输入区域，节省100rpx空间
- 移除底部免责声明条
- AI消息后添加风险提示

用户体验提升:
- 对话内容区从70%提升到85%
- 页面更紧凑、交互更流畅

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 🔍 部署检查清单

### 前端部署
- [ ] Git代码已提交
- [ ] Git代码已推送
- [ ] 本地编译成功
- [ ] 微信开发者工具中测试通过
- [ ] 无控制台错误
- [ ] 所有功能正常工作

### 后端部署
- [ ] Git代码已提交
- [ ] Git代码已推送
- [ ] 服务器代码已更新（git pull）
- [ ] 新依赖已安装（npm install）
- [ ] PM2服务已重启
- [ ] PM2状态显示 online
- [ ] PM2日志无错误
- [ ] API接口测试通过

### 数据库部署
- [ ] SQL脚本已准备
- [ ] 数据库备份已完成
- [ ] SQL脚本已执行
- [ ] 数据验证通过
- [ ] 回滚计划已准备

---

## 💡 常见问题

### Q1: 忘记提交代码怎么办？

**A**: 立即补充提交
```bash
git status
git add .
git commit -m "chore: 补充提交xxx"
git push
```

### Q2: 提交后发现有问题怎么办？

**A**: 使用 `git commit --amend` 修改最后一次提交
```bash
# 修改文件
git add .

# 修改提交信息
git commit --amend

# 如果已经推送，强制推送（谨慎使用）
git push --force-with-lease origin feature/iteration-1
```

### Q3: 如何查看提交历史？

**A**: 使用 `git log`
```bash
# 查看最近5次提交
git log --oneline -5

# 查看详细提交信息
git log -1

# 图形化查看（如果支持）
gitk
```

### Q4: 如何回滚到某个版本？

**A**: 使用 `git reset`
```bash
# 查看提交历史
git log --oneline

# 回滚到某个版本（保留工作区修改）
git reset <commit-hash> --soft

# 回滚到某个版本（丢弃工作区修改）
git reset <commit-hash> --hard

# 强制推送（谨慎）
git push --force-with-lease origin feature/iteration-1
```

---

## 📝 开发流程总结

### 标准开发流程

```
1. 开发功能
   ↓
2. 本地测试
   ↓
3. Git提交
   ↓
4. Git推送
   ↓
5. 更新服务（如果需要）
   ↓
6. 验证功能
   ↓
7. 完成交付
```

### 每次完成交互后，Claude应该：

1. ✅ **检查Git状态**
   ```bash
   git status
   ```

2. ✅ **提交代码**
   ```bash
   git add <files>
   git commit -m "message"
   git push
   ```

3. ✅ **明确告知用户**
   - 代码已提交
   - 代码已推送
   - **是否需要更新服务**
   - **如何更新服务**
   - **如何验证功能**

---

## 🎯 实际应用示例

### 示例1：修改前端页面样式

**Claude应该说**：
```
✅ 页面优化完成！

已提交：
- Commit: abc123
- Branch: feature/iteration-1
- Status: 已推送到远程

更新步骤：
1. 打开微信开发者工具
2. 查看页面效果（编译已自动完成）
3. 测试各项功能

不需要更新：后端服务、数据库
```

### 示例2：添加新的后端API接口

**Claude应该说**：
```
✅ API接口开发完成！

已提交：
- Commit: def456
- Branch: feature/iteration-1
- Status: 已推送到远程

更新步骤（在服务器上执行）：
1. cd /tmp/AITY/backend
2. git pull origin feature/iteration-1
3. npm install  # ← 安装新依赖（如果有）
4. pm2 restart aity-backend
5. pm2 logs aity-backend --lines 20

验证：
- curl http://localhost:8443/api/xxx
- 查看返回结果

需要更新：
✅ 后端服务
✅ 安装新依赖（如果有）
❌ 前端（编译自动完成）
❌ 数据库（除非有SQL变更）
```

### 示例3：修改数据库表结构

**Claude应该说**：
```
✅ 数据库结构更新完成！

已提交：
- Commit: 789abc
- Branch: feature/iteration-1
- Status: 已推送到远程

更新步骤（在服务器上执行）：
1. 备份数据库
   mysqldump -u root -p database_name > backup.sql

2. 执行SQL脚本
   mysql -u root -p database_name < backend/migrations/xxx.sql

3. 如果后端代码改了，重启后端
   cd /tmp/AITY/backend
   pm2 restart aity-backend

需要更新：
✅ 数据库
✅ 后端服务（如果相关代码改了）
❌ 前端（编译自动完成）
```

---

## ⚠️ 重要提示

### 永远不要做的事
- ❌ 修改代码后不提交
- ❌ 提交后不推送
- ❌ 服务器代码更新后不重启服务
- ❌ 新增依赖后不执行 `npm install`
- ❌ 数据库修改前不备份

### 必须要做的事
- ✅ 每次完成后检查 `git status`
- ✅ 确保代码已提交并推送
- ✅ 明确告知用户是否需要更新服务
- ✅ 提供清晰的更新步骤
- ✅ 告知用户如何验证功能

---

**本文档是开发流程的参考标准，每次交互完成时都应遵循此流程！** 📖
