# 🚀 图片上传功能部署指南

**日期**: 2025-02-05
**目的**: 更新服务器代码，启用图片上传功能

---

## ✅ 代码准备情况

### 已完成 ✅

1. **前端代码** (Commit: 49f14ec)
   - ✅ 创建 `src/api/upload.js` (上传API封装)
   - ✅ 修改 `src/pages/create-message/create-message.vue` (自动上传图片)
   - ✅ 修改 `src/utils/request.js` (状态码判断)
   - ✅ **已推送到远程**

2. **后端代码** (Commit: 239d5e9)
   - ✅ 创建 `src/routes/upload.js` (上传路由)
   - ✅ 修改 `src/index.js` (注册上传路由)
   - ✅ **已推送到远程**

### Git状态

```
后端: ✅ 已推送 (239d5e9)
前端: ⏳ 网络超时，可能需要重试或手动同步
```

---

## 🔧 服务器部署步骤

### 第一步：连接服务器

```bash
ssh root@aity88.online
```

### 第二步：进入项目目录

```bash
cd /root/AITY_VIP
```

### 第三步：拉取最新代码

```bash
# 拉取后端代码
cd backend
git pull origin feature/iteration-1

# 查看最新提交
git log --oneline -3

# 应该看到:
# 239d5e9 feat: 添加图片上传接口
# 9e6d582 feat: 修复发布消息400错误
# 861da2d docs: 建立AI协作规则
```

### 第四步：创建上传目录

```bash
# 创建图片上传目录
mkdir -p uploads/images

# 设置权限
chmod 755 uploads/images

# 验证目录
ls -la uploads/
```

**预期输出**:
```
drwxr-xr-x 2 root root 4096 ...  images
```

### 第五步：重启PM2服务

```bash
# 重启后端服务
pm2 restart aity-backend

# 查看服务状态
pm2 status

# 查看最新日志
pm2 logs aity-backend --lines 50
```

**预期输出**:
```
┌────┬─────────────────┬─────────┬─────────┐
│ id │ name            │ version │ mode    │
├────┼─────────────────┼─────────┼─────────┤
│ 1  │ aity-backend    │ 1.0.0   │ fork    │
│    │ Server running  │ online  │ ●       │
└────┴─────────────────┴─────────┴─────────┘
```

### 第六步：验证上传接口

```bash
# 测试上传接口是否正常
curl -X POST https://aity88.online:8443/api/upload \
  -F "file=@/path/to/test/image.jpg" \
  -H "Authorization: Bearer <your-token>"

# 或者直接访问
curl https://aity88.online:8443/api/health
```

**预期响应**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "url": "/uploads/images/img-1738765432-abc123.jpg",
    "filename": "img-1738765432-abc123.jpg",
    "originalname": "test.jpg",
    "size": 12345
  }
}
```

---

## 📱 前端更新（如需要）

### 如果前端代码未自动同步

```bash
# 拉取前端代码
cd /root/AITY_VIP/aity-uni-app-v2
git pull origin feature/iteration-1

# 查看最新提交
git log --oneline -3

# 应该看到:
# 49f14ec feat: 实现图片上传功能，修复附件发布问题
# 6baf938 fix: 修复获取用户信息API方法错误
# 861da2d docs: 建立AI协作规则

# 重新编译小程序
npm run build:mp-weixin
```

---

## 🧪 完整测试流程

### 测试1: 不带附件发布消息

```
1. 打开H5: http://localhost:5173/
2. 登录
3. 创建消息（不带图片）
4. 点击发布
5. 预期: ✅ 成功
```

### 测试2: 带附件发布消息（重点）

```
1. 打开H5: http://localhost:5173/
2. 登录
3. 创建消息
4. 点击"上传图片"，选择一张图片
5. 点击发布

6. 观察流程:
   - 显示"上传图片 1/1" ✅
   - 上传完成后显示"发布成功" ✅
   - 消息出现在列表中 ✅
   - 图片可以正常显示 ✅
```

### 测试3: 多张图片

```
1. 添加3张图片
2. 点击发布

3. 观察流程:
   - "上传图片 1/3" ✅
   - "上传图片 2/3" ✅
   - "上传图片 3/3" ✅
   - "发布成功" ✅
```

---

## 🔍 问题排查

### 问题1: 上传接口404

**症状**:
```
POST https://aity88.online:8443/api/upload 404
```

**解决**:
```bash
# 检查代码是否更新
cd /root/AITY_VIP/backend
git log --oneline -1

# 检查路由文件是否存在
ls -la src/routes/upload.js

# 检查index.js是否包含upload路由
grep "upload" src/index.js

# 如果没有，重新拉取代码
git pull origin feature/iteration-1
pm2 restart aity-backend
```

### 问题2: 文件保存失败

**症状**:
```json
{
  "code": 500,
  "message": "文件保存失败"
}
```

**解决**:
```bash
# 检查上传目录是否存在
ls -la uploads/images

# 如果不存在，创建目录
mkdir -p uploads/images
chmod 755 uploads/images

# 检查磁盘空间
df -h

# 查看PM2日志
pm2 logs aity-backend --lines 50
```

### 问题3: CORS错误

**症状**:
```
Access to XMLHttpRequest blocked by CORS policy
```

**解决**:
```bash
# 检查CORS配置
cd /root/AITY_VIP/backend
grep -A 10 "corsOptions" src/index.js

# 确保包含本地开发地址
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# 重启服务
pm2 restart aity-backend
```

### 问题4: 文件类型不允许

**症状**:
```json
{
  "code": 400,
  "message": "只允许上传图片文件"
}
```

**原因**: 上传的文件不是图片格式

**允许的类型**:
- JPEG (.jpg, .jpeg)
- PNG (.png)
- GIF (.gif)
- WebP (.webp)

---

## 📊 验证清单

### 服务器端

- [ ] 代码已拉取到最新 (git log)
- [ ] 上传目录已创建 (uploads/images)
- [ ] PM2服务已重启 (pm2 status)
- [ ] 服务状态为online (pm2 logs)
- [ ] 上传接口可访问 (curl测试)

### 功能测试

- [ ] 不带附件发布成功
- [ ] 带单个附件发布成功
- [ ] 带多个附件发布成功
- [ ] 图片在消息中正常显示
- [ ] 错误处理正常（上传失败提示）

---

## 🎯 关键文件位置

### 后端文件

```
/root/AITY_VIP/backend/
├── src/
│   ├── index.js           # 主文件（已修改）
│   └── routes/
│       └── upload.js      # 上传路由（新增）
└── uploads/
    └── images/            # 上传目录（需创建）
```

### 前端文件

```
/root/AITY_VIP/aity-uni-app-v2/
└── src/
    ├── api/
    │   └── upload.js      # 上传API（新增）
    └── pages/
        └── create-message/
            └── create-message.vue  # 发布页面（已修改）
```

---

## 🚀 快速部署命令

**一键执行脚本** (在服务器上运行):

```bash
#!/bin/bash

echo "=== 部署图片上传功能 ==="

# 1. 拉取后端代码
echo "步骤1: 拉取后端代码..."
cd /root/AITY_VIP/backend
git pull origin feature/iteration-1

# 2. 创建上传目录
echo "步骤2: 创建上传目录..."
mkdir -p uploads/images
chmod 755 uploads/images

# 3. 重启服务
echo "步骤3: 重启PM2服务..."
pm2 restart aity-backend
sleep 3

# 4. 检查状态
echo "步骤4: 检查服务状态..."
pm2 status

# 5. 查看日志
echo "步骤5: 查看最新日志..."
pm2 logs aity-backend --lines 20

echo "=== 部署完成 ==="
echo "请检查服务状态是否为 online"
```

**保存为脚本执行**:
```bash
# 保存脚本
cat > deploy-upload.sh << 'EOF'
#!/bin/bash
# 上面的脚本内容
EOF

# 添加执行权限
chmod +x deploy-upload.sh

# 执行
./deploy-upload.sh
```

---

## 📞 下一步

### 立即执行

```bash
# 1. 连接服务器
ssh root@aity88.online

# 2. 部署
cd /root/AITY_VIP/backend
git pull origin feature/iteration-1
mkdir -p uploads/images
chmod 755 uploads/images
pm2 restart aity-backend

# 3. 验证
pm2 status
pm2 logs aity-backend --lines 30
```

### 然后测试

```
1. 打开 H5: http://localhost:5173/
2. 登录
3. 创建消息并添加图片
4. 点击发布
5. 观察是否成功
```

---

## ✅ 总结

### 代码准备

- ✅ 前端代码: 已提交并推送
- ✅ 后端代码: 已提交并推送
- ✅ 所有修改: feature/iteration-1分支

### 服务器操作

**唯一需要做的**:
```bash
cd /root/AITY_VIP/backend
git pull origin feature/iteration-1
mkdir -p uploads/images
chmod 755 uploads/images
pm2 restart aity-backend
```

### 功能验证

**测试步骤**:
1. H5创建消息 + 上传图片
2. 点击发布
3. 观察"上传图片 1/1"提示
4. 看到"发布成功"
5. 图片正常显示

---

**现在可以更新服务器了！** 🚀

**部署完成后，图片上传功能就完全可用了！** 📸✨
