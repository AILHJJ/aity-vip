# 🚀 后端服务器部署指南

**日期**: 2025-02-04
**任务**: 更新后端代码（修复发布400错误）
**服务器**: aity88.online

---

## ⚠️ 重要提醒

**必须执行**: 后端代码已修改，必须同步更新服务器，否则小程序功能异常！

---

## 📋 部署步骤

### 第一步：连接到服务器

```bash
ssh root@aity88.online
```

**输入密码**: （你的服务器密码）

---

### 第二步：进入后端目录

```bash
cd /root/AITY_VIP/backend
```

---

### 第三步：拉取最新代码

```bash
git pull origin feature/iteration-1
```

**预期输出**:
```
remote: Counting objects: ...
From https://github.com/AILHJJ/aity-vip
 * branch            feature/iteration-1 -> FETCH_HEAD
Updating 9e6d582..861da2d
Fast-forward
 backend/src/middleware/validation.js             | 2 +-
 backend/src/controllers/messageController.js     | 8 +++++---
 2 files changed, 5 insertions(+), 5 deletions(-)
```

**说明**:
- `validation.js`: groupId改为可选
- `messageController.js`: 自动填充targetGroupId

---

### 第四步：安装依赖（如有新增）

```bash
npm install
```

**说明**: 如果有新的依赖包需要安装

---

### 第五步：重启PM2服务

```bash
pm2 restart aity-vip-backend
```

**预期输出**:
```
[PM2] Applying action restartProcessId on app [aity-vip-backend]
[PM2] [aity-vip-backend](1) ✓ Executed
```

---

### 第六步：查看服务状态

```bash
pm2 status
```

**预期输出**:
```
┌────┬─────────────────────┬─────────────┬─────────┐
│ id │ name                │ version     │ mode    │
├────┼─────────────────────┼─────────────┼─────────┤
│ 1  │ aity-vip-backend    │ 1.0.0       │ fork    │
│    │ Server is running   │ online      │ ●       │
└────┴─────────────────────┴─────────────┴─────────┘
```

**关键**: `status` 必须显示 `online` ✅

---

### 第七步：查看日志（确认无错误）

```bash
pm2 logs aity-vip-backend --lines 50
```

**检查内容**:
- ✅ 无 `ERROR` 级别日志
- ✅ 服务正常启动
- ✅ 监听端口正确（3001）

**正常启动日志示例**:
```
aity-vip-backend | Server running on port 3001
aity-vip-backend | Database connected successfully
```

---

### 第八步：测试API是否正常

#### 方法1：使用curl测试

```bash
curl https://aity88.online:8443/api/health
```

**预期输出**:
```json
{
  "code": 200,
  "message": "Server is running",
  "data": {
    "status": "healthy",
    "timestamp": "2025-02-04T..."
  }
}
```

#### 方法2：浏览器测试

访问: https://aity88.online:8443/api/health

---

## 🔍 验证修复效果

### 测试发布消息功能

1. **打开小程序**（微信开发者工具或真机）

2. **登录管理员账号**
   ```
   Email: admin@example.com
   Password: 123456
   ```

3. **创建新消息**
   - 标题: 测试发布功能
   - 类型: 盘前点评
   - 标签: 全部用户
   - 内容: 测试内容
   - 图片: （可选）

4. **点击发布**

**预期结果**:
- ✅ 发布成功
- ✅ 提示"发布成功"
- ✅ 无400错误
- ✅ 消息出现在列表中

---

## ❌ 常见问题排查

### 问题1: PM2重启失败

**现象**:
```
PM2 command not found
```

**解决**:
```bash
# 安装PM2
npm install -g pm2

# 然后重新启动
pm2 restart aity-vip-backend
```

---

### 问题2: 端口被占用

**现象**:
```
Error: listen EADDRINUSE: address already in use :::3001
```

**解决**:
```bash
# 查找占用端口的进程
lsof -i :3001
# 或
netstat -tulnp | grep 3001

# 杀掉进程
kill -9 <PID>

# 重启服务
pm2 restart aity-vip-backend
```

---

### 问题3: Git拉取失败

**现象**:
```
fatal: unable to access 'https://github.com/...'
```

**解决**:
```bash
# 检查网络连接
ping github.com

# 如果超时，重试几次
git pull origin feature/iteration-1
```

---

### 问题4: 数据库连接失败

**现象**:
```
Error: connect ECONNREFUSED
```

**解决**:
```bash
# 检查MySQL服务
systemctl status mysql

# 启动MySQL
systemctl start mysql

# 重启后端服务
pm2 restart aity-vip-backend
```

---

### 问题5: 仍有400错误

**检查清单**:
- [ ] 确认服务器代码已更新 (`git log` 查看最新commit)
- [ ] 确认PM2已重启 (`pm2 status`)
- [ ] 确认服务正常运行 (`pm2 logs`)
- [ ] 清除小程序缓存重新上传

---

## 📊 部署检查清单

### 完成所有步骤后，请确认：

- [ ] 代码已拉取到服务器 (`git pull` 成功)
- [ ] PM2服务状态为 `online`
- [ ] 日志无ERROR级别错误
- [ ] API测试返回正常（/health接口）
- [ ] 小程序可以正常登录
- [ ] **发布消息功能正常（验证400错误已修复）**

---

## 🎯 完成标志

当你看到以下情况，说明部署成功：

1. ✅ `pm2 status` 显示 `online`
2. ✅ 日志显示 `Server running on port 3001`
3. ✅ 测试发布消息成功
4. ✅ 无400错误

---

## 📞 需要帮助？

如果遇到问题：

1. **查看详细日志**
   ```bash
   pm2 logs aity-vip-backend --lines 100
   ```

2. **查看错误日志**
   ```bash
   pm2 logs aity-vip-backend --err
   ```

3. **联系技术支持**
   - 技术负责人: ___________
   - 紧急联系: ___________

---

## 📝 部署记录

**部署日期**: ___________

**部署人员**: ___________

**部署结果**:
- [ ] 成功
- [ ] 失败（原因: ___________）

**测试结果**:
- [ ] 通过
- [ ] 未通过（问题描述: ___________）

---

**文档版本**: v1.0.0
**创建时间**: 2025-02-04
**适用版本**: v1.6.1

---

## ⚠️ 最后提醒

**完成部署后，一定要测试发布消息功能！**

这是本次修复的核心问题，必须验证400错误已解决。
