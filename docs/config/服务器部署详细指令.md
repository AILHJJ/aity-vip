# 服务器部署详细指令

本文档提供逐步的服务器部署命令，你可以直接复制粘贴执行。

---

## 部署方式选择

### 方式 1: 自动化部署脚本（推荐）

使用我们提供的自动化脚本，一键完成所有配置。

### 方式 2: 手动部署

逐步执行命令，适合需要自定义配置的场景。

---

## 方式 1: 自动化部署（推荐）

### 步骤 1: 上传代码到服务器

在本地执行：

```bash
# 压缩后端代码
cd D:\your-mcp-proxy\AITY_VIP
tar -czf backend.tar.gz backend/

# 上传到服务器（替换 your_server_ip 为实际 IP）
scp backend.tar.gz root@aity88.online:/tmp/
scp scripts/deploy-server.sh root@aity88.online:/tmp/
```

### 步骤 2: 连接服务器

```bash
ssh root@aity88.online
```

### 步骤 3: 解压代码

```bash
cd /tmp
tar -xzf backend.tar.gz
mkdir -p /var/www/aity-vip
mv backend /var/www/aity-vip/
```

### 步骤 4: 运行部署脚本

```bash
chmod +x /tmp/deploy-server.sh
/tmp/deploy-server.sh
```

脚本会自动完成：
- 安装 Node.js, MySQL, PM2, Nginx
- 配置数据库
- 创建环境配置文件
- 安装依赖
- 启动服务
- 配置 Nginx 和防火墙

---

## 方式 2: 手动部署

### 步骤 1: 连接服务器

```bash
ssh root@aity88.online
```

### 步骤 2: 安装 Node.js

```bash
# 安装 Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# 验证安装
node -v
npm -v
```

### 步骤 3: 安装 MySQL

```bash
# 安装 MySQL
apt-get update
apt-get install -y mysql-server

# 启动 MySQL
systemctl start mysql
systemctl enable mysql

# 安全配置
mysql_secure_installation
```

### 步骤 4: 配置数据库

```bash
# 登录 MySQL
mysql -u root -p

# 执行以下 SQL 命令
```

```sql
-- 创建数据库
CREATE DATABASE aity_vip_prod CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户（替换密码）
CREATE USER 'aity_user'@'localhost' IDENTIFIED BY 'your_strong_password';

-- 授权
GRANT ALL PRIVILEGES ON aity_vip_prod.* TO 'aity_user'@'localhost';
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

### 步骤 5: 创建项目目录

```bash
mkdir -p /var/www/aity-vip/backend
cd /var/www/aity-vip
```

### 步骤 6: 上传后端代码

**在本地执行**：

```bash
# 方法 1: 使用 scp
cd D:\your-mcp-proxy\AITY_VIP
scp -r backend/* root@aity88.online:/var/www/aity-vip/backend/

# 方法 2: 使用 Git（如果代码在 Git 仓库中）
# 在服务器上执行：
cd /var/www/aity-vip
git clone your_repository_url backend
```

### 步骤 7: 创建环境配置文件

```bash
cd /var/www/aity-vip/backend
nano .env.production
```

**输入以下内容**（替换密码和密钥）：

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=aity_user
DB_PASSWORD=your_strong_password
DB_NAME=aity_vip_prod

# JWT 配置（使用以下命令生成随机密钥：openssl rand -hex 32）
JWT_SECRET=your_generated_secret_key_here
JWT_EXPIRES_IN=24h

# 服务器配置
PORT=3001
NODE_ENV=production

# CORS 配置
ALLOWED_ORIGINS=https://aity88.online:8443,https://servicewechat.com

# 日志配置
LOG_LEVEL=info
LOG_FILE=/var/log/aity-vip/backend.log
```

**保存并退出**：按 `Ctrl+X`，然后按 `Y`，最后按 `Enter`

**设置文件权限**：

```bash
chmod 600 .env.production
```

### 步骤 8: 安装依赖

```bash
cd /var/www/aity-vip/backend
npm install --production
```

### 步骤 9: 初始化数据库

```bash
NODE_ENV=production node scripts/init-db.js
```

### 步骤 10: 安装 PM2

```bash
npm install -g pm2
```

### 步骤 11: 启动服务

```bash
cd /var/www/aity-vip/backend

# 启动服务
pm2 start src/index.js --name aity-backend --env production

# 设置开机自启
pm2 startup
pm2 save

# 查看状态
pm2 status
pm2 logs aity-backend
```

### 步骤 12: 安装 Nginx

```bash
apt-get install -y nginx
systemctl start nginx
systemctl enable nginx
```

### 步骤 13: 配置 SSL 证书

```bash
# 创建 SSL 目录
mkdir -p /etc/nginx/ssl

# 上传证书文件（在本地执行）
# 假设证书文件在本地的 D:\certs\ 目录
scp D:\certs\aity88.online.crt root@aity88.online:/etc/nginx/ssl/
scp D:\certs\aity88.online.key root@aity88.online:/etc/nginx/ssl/

# 在服务器上设置权限
chmod 600 /etc/nginx/ssl/aity88.online.key
```

**如果没有证书，使用 Let's Encrypt 免费证书**：

```bash
# 安装 Certbot
apt-get install -y certbot python3-certbot-nginx

# 申请证书
certbot --nginx -d aity88.online

# 自动续期
certbot renew --dry-run
```

### 步骤 14: 配置 Nginx

```bash
nano /etc/nginx/sites-available/aity-vip
```

**输入以下内容**：

```nginx
# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name aity88.online;
    return 301 https://$server_name:8443$request_uri;
}

# HTTPS 配置
server {
    listen 8443 ssl http2;
    server_name aity88.online;

    # SSL 证书配置
    ssl_certificate /etc/nginx/ssl/aity88.online.crt;
    ssl_certificate_key /etc/nginx/ssl/aity88.online.key;

    # SSL 安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # 安全头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # API 代理
    location /api/ {
        proxy_pass http://localhost:3001/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # 超时配置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 健康检查
    location /health {
        proxy_pass http://localhost:3001/api/health;
        access_log off;
    }

    # 日志
    access_log /var/log/nginx/aity-vip-access.log;
    error_log /var/log/nginx/aity-vip-error.log;
}
```

**保存并退出**

**启用配置**：

```bash
# 创建软链接
ln -s /etc/nginx/sites-available/aity-vip /etc/nginx/sites-enabled/

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

### 步骤 15: 配置防火墙

```bash
# 安装 UFW（如果未安装）
apt-get install -y ufw

# 允许必要的端口
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw allow 8443/tcp  # 自定义 HTTPS 端口

# 启用防火墙
ufw enable

# 查看状态
ufw status
```

### 步骤 16: 创建日志目录

```bash
mkdir -p /var/log/aity-vip
chown -R www-data:www-data /var/log/aity-vip
```

### 步骤 17: 验证部署

```bash
# 检查服务状态
pm2 status

# 检查健康检查接口
curl http://localhost:3001/api/health

# 检查 HTTPS 访问
curl https://aity88.online:8443/api/health
```

---

## 部署后检查

### 1. 服务状态检查

```bash
# PM2 状态
pm2 status

# 查看日志
pm2 logs aity-backend --lines 50

# Nginx 状态
systemctl status nginx

# MySQL 状态
systemctl status mysql
```

### 2. 网络检查

```bash
# 检查端口监听
netstat -tlnp | grep 3001
netstat -tlnp | grep 8443

# 检查防火墙
ufw status
```

### 3. API 测试

```bash
# 本地测试
curl http://localhost:3001/api/health

# 外部测试
curl https://aity88.online:8443/api/health
```

---

## 常用管理命令

### PM2 管理

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs aity-backend

# 重启服务
pm2 restart aity-backend

# 停止服务
pm2 stop aity-backend

# 删除服务
pm2 delete aity-backend

# 查看详细信息
pm2 show aity-backend

# 监控面板
pm2 monit
```

### Nginx 管理

```bash
# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx

# 重新加载配置
systemctl reload nginx

# 查看状态
systemctl status nginx

# 查看日志
tail -f /var/log/nginx/aity-vip-access.log
tail -f /var/log/nginx/aity-vip-error.log
```

### 数据库管理

```bash
# 登录 MySQL
mysql -u aity_user -p aity_vip_prod

# 备份数据库
mysqldump -u aity_user -p aity_vip_prod > backup_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u aity_user -p aity_vip_prod < backup_20260130.sql
```

---

## 故障排查

### 服务无法启动

```bash
# 查看 PM2 日志
pm2 logs aity-backend --err

# 查看系统日志
journalctl -u pm2-root -n 50

# 检查端口占用
netstat -tlnp | grep 3001
```

### Nginx 502 错误

```bash
# 检查后端服务是否运行
pm2 status

# 检查 Nginx 错误日志
tail -f /var/log/nginx/aity-vip-error.log

# 检查 Nginx 配置
nginx -t
```

### 数据库连接失败

```bash
# 检查 MySQL 状态
systemctl status mysql

# 测试数据库连接
mysql -u aity_user -p -h localhost aity_vip_prod

# 查看 MySQL 日志
tail -f /var/log/mysql/error.log
```

---

## 下一步

部署完成后：

1. **配置微信小程序**
   - 在微信公众平台配置服务器域名
   - 添加 IP 白名单
   - 参考：[小程序自动化部署指南](./小程序自动化部署指南.md)

2. **上传小程序代码**
   - 构建生产版本
   - 上传到微信平台
   - 提交审核

3. **监控和维护**
   - 定期查看日志
   - 监控服务状态
   - 定期备份数据库

---

## 相关文档

- [小程序自动化部署指南](./小程序自动化部署指南.md)
- [生产环境部署指南](./生产环境部署指南.md)
- [本地测试部署指南](./本地测试部署指南.md)

---

**文档更新日期**: 2026-01-30
