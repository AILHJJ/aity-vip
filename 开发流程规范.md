# AITY_VIP 开发流程规范

## 📋 概述

本文档规范了 AITY_VIP 项目的开发、测试、部署全流程，确保开发过程中的数据安全和版本可控。

---

## 🔄 标准开发流程

### 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        开发流程                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1️⃣ 本地开发                                                    │
│     ├── .env.development 配置 DB_ENV=test (测试库)              │
│     ├── npm run dev 启动本地后端                                │
│     ├── npm run dev:mp-weixin 编译小程序前端 ⚠️                  │
│     └── 开发调试功能                                             │
│           ↓                                                      │
│  2️⃣ 测试验证                                                    │
│     ├── 验证功能正常                                             │
│     ├── 检查测试库数据完整性                                     │
│     └── npm run db:status 查看数据库状态                        │
│           ↓                                                      │
│  3️⃣ 同步生产数据 (可选)                                          │
│     ├── npm run db:sync 同步最新生产数据到测试库                 │
│     └── 确保测试库与生产库一致                                   │
│           ↓                                                      │
│  4️⃣ Git 提交                                                    │
│     ├── git add .                                              │
│     ├── git commit -m "feat: 功能描述"                          │
│     └── 推送前确保代码已测试                                     │
│           ↓                                                      │
│  5️⃣ 小程序重新编译                                              │
│     └── npm run build:mp-weixin ⚠️ 必须执行                      │
│           ↓                                                      │
│  6️⃣ 部署到腾讯云                                                │
│     ├── 修改 .env.development DB_ENV=production                 │
│     └── ./deploy.ps1 执行部署                                   │
│           ↓                                                      │
│  7️⃣ 验证生产                                                    │
│     └── 验证生产环境功能正常                                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ 数据库管理

### 数据库架构

| 环境 | 数据库名 | 用途 | 连接配置 |
|------|----------|------|----------|
| 测试库 | `投研图灵室_test` | 本地开发、测试 | .env.development |
| 生产库 | `投研图灵室` | 正式环境 | .env.production |

### 数据库配置

**测试库连接信息：**
```yaml
Host: 124.221.119.134
Port: 3306
Database: 投研图灵室_test
User: 投研图灵室
Password: fl10b312
```

**生产库连接信息：**
```yaml
Host: 124.221.119.134
Port: 3306
Database: 投研图灵室
User: 投研图灵室
Password: fl10b312
```

### 数据库操作命令

```bash
# 查看数据库状态
npm run db:status

# 查看测试库详情
npm run db:verify

# 同步生产数据到测试库（⚠️ 会覆盖测试库数据）
npm run db:sync

# 预演同步（不实际执行）
npm run db:sync:dry

# 初始化数据库
npm run db:init
```

### 数据库变更流程

> ⚠️ **重要**：任何数据库结构变更都需要谨慎操作

#### 添加新表/字段

1. **本地创建迁移脚本**
   ```bash
   # 在 migrations/ 目录创建 SQL 文件
   migrations/
   └── v1_x_x_create_xxx_table.sql
   ```

2. **宝塔面板执行迁移**
   - 登录 phpMyAdmin：https://124.221.119.134:8888/phpmyadmin
   - 先在**测试库**执行 SQL
   - 验证无误后再在**生产库**执行

3. **记录变更**
   - 更新本文档
   - 在迭代记录中标注数据库变更

#### 数据迁移

```sql
-- 复制表结构
CREATE TABLE 新库名.新表 LIKE 原库名.原表;

-- 复制数据
INSERT INTO 新库名.新表 SELECT * FROM 原库名.原表;

-- 复制特定数据
INSERT INTO 新库名.新表 SELECT * FROM 原库名.原表 WHERE 条件;
```

---

## 🚀 本地开发

### 环境准备

1. **安装后端依赖**
   ```bash
   cd backend
   npm install
   ```

2. **配置环境变量**
   ```bash
   # 查看配置
   cat .env.development
   ```

3. **启动本地后端**
   ```bash
   npm run dev
   ```

### 小程序开发 ⚠️ 重要

> **每次前端代码更新后，必须重新编译小程序！**

#### UniApp 小程序编译

```bash
# 进入小程序目录
cd ../aity-uni-app-v2

# 安装依赖（如有更新）
npm install

# 本地开发编译（连接 localhost:3001）
npm run dev:mp-weixin:local

# 云端编译（连接 aity88.online:8443）
npm run dev:mp-weixin:cloud

# 生产编译（发布用）
npm run build:mp-weixin:local   # 本地API
npm run build:mp-weixin:cloud   # 云端API
```

#### HBuilderX 编译

1. 打开 HBuilderX
2. 打开项目 `aity-uni-app-v2`
3. 运行 → 运行到小程序模拟器 → 微信开发者工具
4. 代码修改后会自动热更新，但涉及环境变量修改需重新编译

#### 小程序测试流程

```
前端代码更新 → 重新编译小程序 → 微信开发者工具刷新 → 测试功能
                     ↑
              ⚠️ 必须执行！
```

#### 前端API地址配置

开发时需修改前端请求地址为本地：
- 文件：`src/utils/request.js` 或 `src/api/config.js`
- 改为：`http://localhost:3001`

### 常用开发命令

```bash
# 后端相关
cd backend
npm run dev          # 启动开发服务器 (连接测试库)
npm run dev:prod     # 启动开发服务器 (连接生产库)
npm start            # 生产环境启动

# 小程序相关
cd ../aity-uni-app-v2
npm run dev:mp-weixin    # 开发编译（微信小程序）
npm run build:mp-weixin  # 生产编译

# 数据库相关
cd backend
npm run db:status    # 查看数据库状态
npm run db:verify    # 验证测试库
npm run db:sync      # 同步生产数据到测试库

# Git相关
npm run git:log      # 查看提交历史
npm run git:status   # 查看文件状态
```

### 一键启动脚本（后端 + 前端）

```bash
# Windows
./start-dev.ps1

# Linux/Mac
./start-dev.sh
```

---

## 📦 部署流程

### 一键部署（推荐）

```bash
# Windows
./deploy.ps1

# Linux/Mac
./deploy.sh
```

### 手动部署

1. **登录腾讯云服务器**
   ```bash
   ssh root@124.221.119.134
   ```

2. **拉取最新代码**
   ```bash
   cd /www/wwwroot/AITY_VIP
   git pull
   ```

3. **安装依赖**
   ```bash
   cd backend
   npm install
   ```

4. **重启服务**
   ```bash
   pm2 restart ecosystem.config.js --env production
   ```

5. **验证部署**
   ```bash
   curl http://localhost:3001/api/health
   ```

### 部署检查清单

- [ ] 代码已提交 Git
- [ ] 后端本地测试通过
- [ ] **小程序已重新编译**
- [ ] 数据库变更已在测试库验证
- [ ] 数据库变更已在生产库执行
- [ ] 部署脚本执行成功
- [ ] 生产环境功能正常

---

## 🔙 版本回滚

### Git 回滚

```bash
# 查看最近10次提交
git log --oneline -10

# 回滚到指定版本
git reset --hard <commit_id>

# 回滚上一个版本
git reset --hard HEAD~1

# 强制推送（⚠️ 谨慎使用）
git push --force
```

### 数据库回滚

> ⚠️ **警告**：数据库回滚可能导致数据丢失

```sql
-- 查看表结构变更历史（需要开启审计）
-- 从备份恢复（宝塔面板 → 数据库 → 备份恢复）
```

### 一键回滚脚本（待实现）

```bash
# 回滚到指定版本
./rollback.ps1 v1.7.0
```

---

## 📝 迭代记录

每次迭代需要记录：

| 项目 | 说明 |
|------|------|
| 版本号 | v1.x.x |
| 日期 | YYYY-MM-DD |
| 功能变更 | 新增/修改/删除的功能 |
| 数据库变更 | 新增表/字段/数据 |
| 文件变更 | 新增/修改的文件列表 |
| 负责人 | 记录操作人 |
| 备注 | 其他注意事项 |

### 迭代记录文件位置

```
AITY_VIP/docs/
├── 迭代记录_v1.8.0.md
├── 迭代记录_v1.7.0.md
└── ...
```

---

## ⚠️ 注意事项

1. **绝对不要在本地开发环境连接生产库进行测试**
   - .env.development 的 DB_ENV 必须保持为 test

2. **数据库变更必须先在测试库验证**
   - 新表、新字段先在测试库创建
   - 确认无误后再操作生产库

3. **重要操作必须留痕**
   - 所有数据库变更记录在迭代记录中
   - Git 提交信息必须清晰描述变更内容

4. **部署前必须本地测试通过**
   - 确保功能在本地测试库验证无误
   - 再进行生产环境部署

5. **保持本地代码与远程同步**
   - 每次开发前先 git pull
   - 提交前确保代码无冲突

---

## 🔧 常见问题

### Q: 测试库连接失败？

```bash
# 检查IP授权
# 在宝塔面板执行：
GRANT ALL PRIVILEGES ON `投研图灵室_test`.* TO '投研图灵室'@'%';
FLUSH PRIVILEGES;
```

### Q: 部署后功能异常？

```bash
# 1. 检查日志
pm2 logs

# 2. 重启服务
pm2 restart all

# 3. 检查环境变量
pm2 env
```

### Q: 如何同步最新生产数据到测试库？

```bash
npm run db:sync
```

---

## 📞 联系方式

- **服务器地址**: 124.221.119.134
- **宝塔面板**: https://124.221.119.134:8888
- **phpMyAdmin**: https://124.221.119.134:8888/phpmyadmin

---

**最后更新**: 2026-04-24
**当前版本**: v1.8.0
