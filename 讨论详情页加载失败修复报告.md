# 讨论详情页加载失败修复报告

## 问题描述
用户点击回帖进入讨论详情页时，虽然API返回200成功，但页面一直显示"加载失败"。

## 根本原因分析

### 1. API响应格式不匹配

**后端返回格式**（统一响应格式）：
```json
{
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

**前端错误判断**（使用错误的字段）：
```javascript
if (res.success) {  // ❌ 错误：res.success 永远是 undefined
  discussion.value = res.data
}
```

**正确判断应该是**：
```javascript
if (res.code === 200 && res.data) {  // ✓ 正确
  discussion.value = res.data
}
```

### 2. request.js 的响应处理

在 `request.js` 中（第126-128行），当状态码为200-299时：
```javascript
if (res.statusCode >= 200 && res.statusCode < 300) {
  resolve(res.data)  // 返回整个响应数据对象
}
```

所以前端收到的 `res` 是：
```javascript
{
  code: 200,
  message: "Success",
  data: { ... }
}
```

而不是：
```javascript
{
  success: true,
  data: { ... }
}
```

### 3. 后端接口不一致问题

`getDiscussionReplies` 接口直接返回原始数据，没有包含用户信息：
```javascript
// 修复前（第415行）
res.json(success(replies));  // ❌ 缺少 userName 字段

// 修复后
const repliesWithSender = await Promise.all(
  replies.map(async (reply) => {
    const sender = await User.findByPk(reply.senderId, {
      attributes: ['name', 'avatar']
    });
    return {
      ...reply.toJSON(),
      userName: sender?.name || '匿名用户',
      userAvatar: sender?.avatar
    };
  })
);
res.json(success(repliesWithSender));  // ✓ 包含 userName 字段
```

## 修复方案

### 前端修复（discussion-detail.vue）

#### 1. 修复 loadDiscussion 函数（第159-205行）
```javascript
// 修复前
if (res.success) {  // ❌

// 修复后
if (res.code === 200 && res.data) {  // ✓
```

#### 2. 修复 loadReplies 函数（第211-236行）
```javascript
// 修复前
if (res.success) {
  replies.value = res.data.replies || res.data.list || []
}

// 修复后
if (res.code === 200) {
  replies.value = res.data || []  // 后端直接返回数组
}
```

#### 3. 修复 handleReply 函数（第241-287行）
```javascript
// 修复前
if (res.success) {

// 修复后
if (res.code === 200) {
```

#### 4. 修复 handleVisibilityChange 函数（第305-340行）
```javascript
// 修复前
if (res.success) {

// 修复后
if (res.code === 200) {
```

### 后端修复（discussionController.js）

#### 修复 getDiscussionReplies 函数（第398-439行）

添加了用户信息关联查询，确保返回 `userName` 字段：

```javascript
// 获取每个回复的发送者信息
const repliesWithSender = await Promise.all(
  replies.map(async (reply) => {
    const sender = await User.findByPk(reply.senderId, {
      attributes: ['name', 'avatar']
    });
    return {
      ...reply.toJSON(),
      userName: sender?.name || '匿名用户',
      userAvatar: sender?.avatar
    };
  })
);

console.log(`[获取讨论回复] 成功 - 讨论ID: ${id}, 回复数: ${repliesWithSender.length}`);

res.json(success(repliesWithSender));
```

## 字段映射关系

### 后端返回的字段（统一使用驼峰命名）

**讨论详情**：
```javascript
{
  id: 1,
  messageId: 1,
  userId: 1,
  creatorId: 1,        // ✓ 发起者ID
  creatorName: "...",  // ✓ 发起者名称
  userName: "...",     // ✓ 用户名称（与creatorName相同）
  userAvatar: "...",
  title: "...",
  content: "...",
  status: "pending",
  visibility: "private",
  replyCount: 0,
  viewCount: 0,
  createdAt: "...",
  updatedAt: "..."
}
```

**回复列表**：
```javascript
[
  {
    id: 1,
    discussionId: 1,
    senderId: 1,
    senderName: "...",
    userName: "...",   // ✓ 发送者名称（与senderName相同）
    userAvatar: "...",
    content: "...",
    createdAt: "...",
    updatedAt: "..."
  }
]
```

### 前端使用的字段

**讨论详情页**（第38行）：
```vue
<text class="user-name">{{ discussion.creatorName }}</text>  // ✓ 正确
```

**回复列表**（第84行）：
```vue
<text class="reply-user">{{ reply.userName }}</text>  // ✓ 正确
```

## 修复验证

### 1. 检查API响应格式
```bash
curl -X GET "http://localhost:3000/api/discussions/1" \
  -H "Authorization: Bearer <token>"
```

期望响应：
```json
{
  "code": 200,
  "message": "Success",
  "data": {
    "id": 1,
    "creatorName": "用户名",
    "userName": "用户名",
    ...
  }
}
```

### 2. 检查前端判断逻辑
打开浏览器控制台，应该看到：
```
[讨论详情] API响应: {code: 200, message: "Success", data: {...}}
[讨论详情] 加载成功
[讨论回复] API响应: {code: 200, message: "Success", data: [...]}
[讨论回复] 加载成功，回复数: 0
```

### 3. 页面显示正常
- 不再显示"加载失败"
- 正常显示讨论内容
- 正常显示回复列表
- 可以正常发送回复

## 相关文件

### 前端
- `aity-uni-app-v2/src/pages/discussion-detail/discussion-detail.vue`
- `aity-uni-app-v2/src/api/discussion.js`
- `aity-uni-app-v2/src/utils/request.js`

### 后端
- `backend/src/controllers/discussionController.js`

## 总结

**问题核心**：前端使用了错误的响应字段 `res.success`，而后端返回的是 `res.code`。

**修复要点**：
1. 将所有 `res.success` 判断改为 `res.code === 200`
2. 后端 `getDiscussionReplies` 接口添加用户信息关联
3. 统一前后端的数据格式和字段命名

**影响范围**：
- 讨论详情加载
- 回复列表加载
- 发送回复功能
- 修改可见性功能

所有使用 `res.success` 判断的地方都已修复为 `res.code === 200`。
